{%- comment -%} Generic content page (About, FAQ, Contact, Shipping, etc.). {%- endcomment -%}
<section class="mx-auto max-w-3xl px-6 pb-16 pt-28 lg:px-8 lg:pt-36">
  <h1 class="font-display text-4xl tracking-[-0.03em] text-[var(--color-ink)] sm:text-5xl">{{ page.title }}</h1>
  <div class="prose mt-8 max-w-none text-sm leading-7 text-[var(--color-muted)]">
    {{ page.content }}
  </div>
</section>
